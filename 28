#include <iostream>
using namespace std;

int main() {
    int Sum = 1;
    for (int n = 3; n <= 1001; n += 2) {
        int sum = n*n + (n*n-(n-1)) + (n*n-2*(n-1)) + (n*n-3*(n-1));
        Sum += sum;
    }
    cout << Sum << endl;
    return 0;
}