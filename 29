#include <iostream>
#include <cmath>
using namespace std;

int main() {
    long double logs[10000];
    int count = 0;

    for (int a = 2; a <= 100; ++a) {
        long double loga = log(a);
        for (int b = 2; b <= 100; ++b) {
            long double x = b * loga;
            bool exists = false;
            for (int i = 0; i < count; ++i) {
                if (fabs(logs[i] - x) < 1e-12) {
                    exists = true;
                    break;
                }
            }
            if (!exists) logs[count++] = x;
        }
    }

    cout << count << endl;
    return 0;
}